# Local / OS
.DS_Store
Thumbs.db
.directory
*.log

# Secrets and machine-specific (never publish)
**/.env
**/.env.*
!**/.env.example
**/local.properties
**/*.jks
**/*.keystore
**/*.pem
android/
# Android local build trees
android/.gradle/
android/.idea/
android/.cxx/
android/build/
android/app/build/
android/captures/

# Windows local build trees
windows/node_modules/
windows/dist/
windows/src-tauri/target/
windows/src-tauri/gen/

# Packages — except the stable Windows installer and Android APK GitHub serves
*.exe
*.apk
*.aab
*.msi
!windows/OpenConnect +P_latest_x64-setup.exe
!android/OpenConnect-P_latest.apk

# Signing secrets (keep the .example files)
**/release.properties
!**/release.properties.example
