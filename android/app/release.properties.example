# Copy to release.properties next to this file and fill in the password.
# Do not commit release.properties or your .jks file.
# Put myoc-release.jks in android/app/ (same folder as this example).
path=myoc-release.jks
alias=myoc
password=YOUR_KEYSTORE_PASSWORD
